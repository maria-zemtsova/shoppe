{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "types": ["nuxt", "vite/client"],
    "strict": true,
    "moduleResolution": "bundler"
  },

  "include": ["**/*.ts", "**/*.vue", ".nuxt/types/**/*.d.ts", "types/**/*.ts", "stores/cart.ts"],
  "exclude": ["node_modules", ".nuxt", ".output", "dist"]

}
