{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "types": ["nuxt", "vite/client"],
    "strict": true,
    "moduleResolution": "bundler"
  },
  "include": ["**/*.ts", "**/*.vue", ".nuxt", ".nuxt/types/**/*.d.ts", "types/**/*.ts"],
  "exclude": ["node_modules", ".output", "dist"]
}
